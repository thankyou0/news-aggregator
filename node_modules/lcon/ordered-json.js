/// <reference path="typings/underscore.d.ts" />
var _ = require('underscore');

function orderedToUnordered(orderedJson) {
    if (_.isArray(orderedJson)) {
        if (orderedJson[0]) {
            return _(orderedJson.slice(1)).map(exports.orderedToUnordered);
        } else {
            var object = new Object(null);
            for (var i = 1; i < orderedJson.length; i += 2) {
                object[orderedJson[i]] = exports.orderedToUnordered(orderedJson[i + 1]);
            }
            return object;
        }
    } else
        return orderedJson;
}
exports.orderedToUnordered = orderedToUnordered;
//# sourceMappingURL=ordered-json.js.map
